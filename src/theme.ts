// Ant Design 主题配置。
// 说明：正式集成中主题由成员1在共享工程统一维护，本工程提供等价基础主题用于自测。
import type { ThemeConfig } from 'antd';

export const appTheme: ThemeConfig = {
  token: {
    colorPrimary: '#1f5eff',
    borderRadius: 6,
    fontSize: 14,
  },
};
