# 成员4准备报告与 Cursor 构建提示词

本文档由以下两部分组成：

1. 成员4开发准备报告；
2. 成员4 Cursor 完整构建提示词。

---

# 企业级混合检索 RAG 项目成员4开发准备报告

## 一、结论

你负责的是全项目唯一的身份、组织、RBAC、数据范围与权限计算后端。成员4不是只完成登录接口，而是要建立所有业务模块共同使用的权限边界，使用户在功能调用、知识检索、文档访问、标准问答、引用打开和缓存复用等环节都只能接触被授权的数据。

成员4模块只有同时满足以下条件，才可视为“可执行、可落地、可交付”：

1. 用户登录、Token 刷新、退出、撤销、禁用和登录限制可真实运行；
2. 用户、部门、用户组、角色、权限和组织关系可管理；
3. RBAC、数据范围、基础 ABAC、显式拒绝和临时授权按统一规则计算；
4. 全项目只存在一套 `PermissionService`，其他成员不得复制权限逻辑；
5. 每次业务请求可获得不可变的 `AccessContext` 快照；
6. 权限可以在检索前转换为 OpenSearch 与 pgvector 可消费的统一语义过滤条件；
7. 文档、Chunk、标准问答、引用和缓存使用同一权限上下文；
8. 权限变化通过数据库事务和 Outbox 事件触发缓存失效与审计；
9. 无权限、越权访问、ID 猜测、过期临时授权等安全用例全部被拒绝并留下审计记录；
10. 源代码、迁移、测试、接口文档、配置说明、演示数据、验收记录和 Pull Request 齐全。

## 二、必须先统一的仓库理解

上传任务书规定的协作模式是：

```text
七名成员使用各自 Gitea 账号
→ 克隆组长的同一个私有仓库
→ 远程仓库名称统一为 origin
→ 从 develop 创建个人功能分支
→ 通过 Pull Request 合并到 develop
→ main 仅用于正式发布
```

团队共享仓库预期地址为：

```text
http://192.168.51.25:3010/admin/origin.git
```

成员4开始前应执行：

```bash
git status --short
git branch --show-current
git remote -v
git fetch origin
```

预期 `origin` 的 fetch 和 push 地址均指向上述共享仓库。不得因为自己是成员4而重新建立一个独立仓库，也不得使用组长账号或共享访问令牌。

禁止执行：

```bash
git init
git remote add origin ...
git remote set-url origin ...
git push --force
git push -f
git reset --hard
git clean -fd
```

如果 `origin` 与组长地址不一致，必须停止 Git 写操作并先由组长确认。若团队实际采用“每人一个完全独立仓库”，则与任务书中的 `develop`、分支保护和统一 Pull Request 流程冲突，需要先由组长修正协作方式。

## 三、成员4在项目中的定位

### 3.1 你必须负责的内容

| 工作域 | 具体职责 |
|---|---|
| 身份认证 | 用户名密码登录、密码安全哈希、Access Token、Refresh Token、轮换或撤销、退出、当前用户、禁用、密码重置、失败次数限制、登录日志和 SSO 扩展接口 |
| 组织管理 | 用户、部门、部门层级、用户组、主部门、用户组成员关系、用户状态和批量导入预留接口 |
| RBAC | 角色、权限、用户角色、角色权限、菜单权限、API 权限、操作权限和稳定权限编码 |
| 数据权限 | tenant、部门、用户组、知识库、项目、地区、文档密级、生效时间、失效时间、显式拒绝和临时授权 |
| 权限计算 | 默认拒绝、显式拒绝优先、功能权限与数据权限分离、多角色合并、临时授权有效期、密级限制和继承规则 |
| AccessContext | 为每次请求计算不可变权限快照，生成不可逆的 `scope_hash`，用于检索和缓存隔离 |
| PermissionService | 提供全项目唯一的权限查询、动作校验、资源校验、检索过滤、引用校验和缓存失效能力 |
| 检索前过滤 | 将统一权限语义转换为成员5、成员6可使用的 OpenSearch Filter DSL 和 PostgreSQL/pgvector WHERE 条件 |
| 审计与安全 | `AuditLog`、`SecurityEvent`、登录审计、权限审计、越权事件结构、存储和查询接口 |
| 权限事件 | 在同一数据库事务写入 Outbox，发布权限变化和临时授权过期事件，推动权限缓存和问答缓存失效 |
| 跨模块接口 | 向成员2、3、5、6、7提供稳定接口和数据契约，并完成联调和安全测试 |

### 3.2 不属于你的主责

| 不属于成员4的内容 | 正确协作方式 |
|---|---|
| 文档上传、解析、切分、Embedding 和索引写入 | 向成员5提供权限绑定、继承和访问校验能力，不修改成员5核心流程 |
| Keyword、Vector、RRF、Reranker 和 RAG | 向成员6输出 AccessContext 与检索过滤，不实现检索算法 |
| 标准问答审核、发布和评估 | 向成员7提供标准问答权限校验和越权安全事件，不负责内容审核 |
| 用户端和管理后台界面 | 向成员2、3提供接口、菜单和权限数据，不修改其页面核心逻辑 |
| 公共 API、错误码、事件信封和基础中间件 | 与成员1共同评审，不能私自改变公共契约 |
| Prometheus、Grafana 和最终部署拓扑 | 产生本模块指标，由成员7聚合；公共部署由成员1审核 |

## 四、编码前必须准备的内容

## 4.1 本地开发环境

先确认：

- Python 3.10；
- Docker 与 Docker Compose；
- Git；
- 当前仓库可以正常读取；
- 当前用户自己的 Gitea 凭据可推送个人功能分支；
- PostgreSQL、Redis 和项目公共后端环境可以启动；
- 仓库已有 FastAPI、SQLAlchemy 2.x、Alembic、Celery、日志和测试基础设施。

不得在成员4模块内重复创建第二套数据库连接、配置系统、异常处理、日志格式或认证中间件。

## 4.2 从成员1取得并签收的公共契约

正式编码前，应确认仓库中已经存在或由成员1发布以下内容：

- `AGENTS.md` 或等价项目规则；
- API 路径和版本规范；
- 统一成功、失败响应格式；
- 公共错误码；
- ID、时间、分页和软删除规范；
- 公共状态枚举；
- SQLAlchemy Base、Session 与 Alembic 迁移规则；
- 认证依赖和 FastAPI 中间件接口；
- `request_id`、`trace_id` 和结构化日志规范；
- 审计调用接口；
- Outbox 事件信封和幂等消费规则；
- Prometheus 指标命名规范；
- 配置、密钥读取和脱敏规范；
- Docker Compose 与健康检查规则。

公共契约缺失时可以先实现成员4内部逻辑和契约一致的测试替身，但不得私自冻结全项目公共字段。

## 4.3 必须由团队确认、不能凭空设定的业务参数

任务书明确指出以下参数需要启动时确认：

- 文档密级枚举和等级顺序；
- tenant、部门、项目、地区等实际 ABAC 属性；
- 一期实际角色和初始权限编码清单；
- 登录失败阈值、限制时长和 Token 生命周期；
- Refresh Token 轮换或失效的具体策略；
- 临时授权可允许的资源类型和审批流程；
- 权限变化对正在进行请求的既定处理策略；
- 首批管理员和测试账号；
- 生产环境安全、性能和审计保留要求。

Cursor 不得自行编造企业组织、真实人员、密级名称、部门关系或生产安全参数。仓库已有配置优先；仍缺失时，应集中生成“待团队确认参数清单”，同时继续完成不依赖具体数值的实现。

## 4.4 需要冻结的数据模型

至少包含：

```text
Tenant
User
Department
UserGroup
Role
Permission
UserRole
RolePermission
UserDepartment
UserGroupMember
DataScope
KnowledgeBasePermission
DocumentPermission
TemporaryGrant
LoginLog
AuditLog
SecurityEvent
```

所有权限实体至少考虑：

- `tenant_id`；
- 状态；
- 生效时间；
- 失效时间；
- 创建人；
- 更新人；
- 创建时间；
- 更新时间；
- 必要的软删除字段。

还需要与成员1、成员5确认公共实体外键、ID 类型、状态枚举、删除策略和迁移归属，避免成员4自行复制 KnowledgeBase、Document 等业务实体。

## 4.5 必须发布的 AccessContext 契约

任务书要求至少兼容：

```json
{
  "tenant_id": "",
  "user_id": "",
  "role_ids": [],
  "department_ids": [],
  "group_ids": [],
  "knowledge_base_ids": [],
  "project_ids": [],
  "regions": [],
  "max_confidentiality_level": 0,
  "deny_document_ids": [],
  "temporary_grants": [],
  "scope_hash": ""
}
```

关键规则：

- 每个请求使用不可变快照；
- `scope_hash` 只用于权限范围标识和缓存隔离；
- `scope_hash` 不得包含可逆敏感数据；
- 权限变更后必须使旧缓存失效；
- 系统管理员不自动获得所有业务文档阅读权。

## 4.6 必须实现的 PermissionService 能力

```text
get_access_context()
get_user_access_scope()
build_retrieval_filters()
can_execute_action()
can_access_knowledge_base()
can_access_document()
can_access_chunk()
can_access_standard_qa()
can_open_citation()
invalidate_user_permission_cache()
```

其他模块必须调用这些能力，不得直接查询权限表后自行推导规则。

## 五、跨成员协作准备

### 5.1 与成员1

共同冻结：

- 认证依赖和中间件；
- 公共响应、错误码、分页、时间和状态；
- 审计事件结构；
- Outbox 事件格式；
- 数据库迁移边界；
- 公共路由注册和依赖修改；
- `request_id`、`trace_id` 与日志字段。

公共目录、根目录依赖和 Docker 配置的任何修改都必须邀请成员1评审。

### 5.2 向成员2提供

- `POST /api/v1/auth/login`；
- `POST /api/v1/auth/refresh`；
- `POST /api/v1/auth/logout`；
- `GET /api/v1/auth/me`；
- 当前用户角色和权限摘要；
- 当前用户可访问知识库摘要；
- 临时授权及到期时间；
- 认证失效、用户禁用和权限不足的公共错误码。

成员2只能展示后端结果，不在前端重新推导权限。

### 5.3 向成员3提供

- 用户、部门、用户组 CRUD；
- 角色、权限、用户角色和角色权限配置；
- 功能权限和数据权限配置；
- 临时授权管理；
- 当前管理员菜单和操作权限；
- 用户最终权限摘要；
- 登录、权限和安全审计查询；
- 分页、搜索、筛选、冲突和操作权限结果。

菜单和按钮隐藏只改善体验，后端 API 仍必须执行真实鉴权。

### 5.4 向成员5提供

- 知识库默认权限；
- 文档权限绑定；
- 文档权限变化校验；
- Chunk 继承文档权限规则；
- 文档访问校验；
- 索引所需权限元数据语义；
- `permission.knowledge_base.changed`、`permission.document.changed` 等事件。

成员5负责把权限元数据写入 Chunk 和索引，但不自行定义权限规则。

### 5.5 向成员6提供

- 不可变 `AccessContext`；
- `build_retrieval_filters()` 的统一语义过滤结果；
- OpenSearch 与 pgvector 适配所需的权限字段说明；
- `can_access_chunk()`；
- `can_access_standard_qa()`；
- `can_open_citation()`；
- `scope_hash` 和缓存权限摘要；
- 权限变更与临时授权过期事件。

成员6必须在检索前使用过滤条件，并在引用返回前二次校验，不得自行解释复杂 RBAC 或 ABAC 规则。

### 5.6 向成员7提供

- 标准问答权限校验；
- 标准问答适用角色、部门和禁止角色的校验能力；
- 评估任务所需权限上下文；
- 越权召回、越权引用、越权回答和跨权限缓存泄露的安全事件数据；
- 权限相关 Prometheus 指标。

成员7负责监控聚合，不替代成员4的审计和安全事件存储。

## 六、建议开发顺序

## M1：认证与组织

完成：

1. 审计仓库和公共契约；
2. 冻结身份与组织实体；
3. 创建可升级、可回退的 Alembic 迁移；
4. 实现密码安全存储、登录、Token、刷新、退出和当前用户；
5. 实现用户、部门、部门层级、用户组和组织关系；
6. 实现登录失败限制、用户禁用和登录日志；
7. 完成成员2登录联调所需接口和测试。

建议分支：

```text
feature/m4-auth-organization
```

## M2：RBAC

完成：

1. 角色、权限、用户角色、角色权限；
2. 菜单、API 和操作权限；
3. 稳定权限编码；
4. `can_execute_action()`；
5. 管理接口、引用检查和审计；
6. 成员3菜单、按钮和权限中心联调。

建议分支：

```text
feature/m4-rbac
```

## M3：数据范围、ABAC 和 PermissionService

完成：

1. DataScope；
2. KnowledgeBasePermission；
3. DocumentPermission；
4. 密级、部门、用户组、项目、地区和时间约束；
5. 显式拒绝；
6. TemporaryGrant；
7. AccessContext；
8. `scope_hash`；
9. PermissionService 全部方法；
10. 权限缓存及失效。

建议分支：

```text
feature/m4-data-scope-permission-service
```

## M4：检索、引用、标准问答与审计联调

完成：

1. 向成员5冻结权限继承和索引权限元数据；
2. 向成员6冻结检索过滤、引用校验和缓存隔离；
3. 向成员7冻结标准问答权限与安全事件；
4. 实现 AuditLog、SecurityEvent 和查询接口；
5. 实现权限变化 Outbox 事件；
6. 完成全链路越权测试和缓存失效测试；
7. 完成演示、验收和 Pull Request。

建议分支：

```text
feature/m4-permission-integration-audit
```

每个分支只处理一个明确里程碑，通过 Pull Request 合并到 `develop`，不要将整个模块积压到最后一次提交。

## 七、权限计算必须遵守的规则

1. 默认拒绝；
2. 显式拒绝优先；
3. 功能权限和数据权限分别计算；
4. 多角色允许范围合并；
5. 临时授权仅在有效期内生效；
6. 文档密级不得高于用户可访问等级；
7. Chunk 权限不得高于来源文档；
8. 标准问答权限不得高于全部来源文档；
9. 引用打开时重新校验权限；
10. 权限变更后使相关权限、查询和答案缓存失效；
11. 系统管理员不默认拥有全部业务文档阅读权；
12. 任何资源 ID 猜测都不能绕过权限检查。

## 八、需要准备的测试数据

任务书要求至少建立：

- 两个部门；
- 三种角色；
- 公共知识库；
- 部门私有知识库；
- 显式拒绝文档；
- 有效临时授权；
- 过期临时授权；
- 不同文档密级；
- 禁用用户；
- 多角色用户。

演示数据必须为虚构测试数据，不得包含真实企业用户、真实密码、真实密钥或生产数据。

## 九、必须通过的测试与验收

### 9.1 认证安全测试

- 无 Token 访问受保护接口；
- Token 过期；
- Refresh Token 被撤销；
- Refresh Token 轮换或失效；
- 用户禁用后现有会话失效；
- 连续登录失败达到阈值；
- Token 不进入普通日志；
- 退出后 Token 或会话不能继续使用。

### 9.2 功能权限测试

- 普通用户访问后台管理 API；
- 无权限菜单和操作接口；
- 角色删除前存在引用；
- 权限变化审计；
- 多角色功能权限合并；
- 系统管理员没有业务阅读权限时读取文档。

### 9.3 数据权限测试

- A 部门用户访问 B 部门文档；
- 公共知识库访问；
- 部门私有知识库访问；
- 显式拒绝优先；
- 临时授权有效；
- 临时授权过期；
- 密级超过用户上限；
- Chunk 权限继承；
- 标准问答来源权限；
- 引用二次权限校验；
- 构造 ID 猜测资源；
- 权限变化后的缓存失效。

### 9.4 验收目标

- 无权限默认拒绝；
- 显式拒绝优先；
- OpenSearch 与 pgvector 使用同一权限语义；
- 文档、Chunk、标准问答和引用权限一致；
- 越权事件记录完整；
- 权限变更及时生效；
- 越权召回次数为零；
- 越权引用次数为零；
- 越权回答次数为零；
- 跨权限缓存泄露次数为零。

## 十、最终交付清单

成员4完成时至少提交：

1. 身份、组织、RBAC、数据范围、权限服务和审计源代码；
2. 可升级和可回退的数据库迁移；
3. API 路由、Schema、Service 和依赖注入；
4. `AccessContext` 与检索过滤契约；
5. Outbox 事件和缓存失效实现；
6. 单元测试、权限矩阵测试和跨模块集成测试；
7. 结构化日志、登录审计、权限审计和安全事件；
8. 本模块 Prometheus 指标；
9. API 文档和错误码说明；
10. 配置与环境变量说明；
11. 模块 README；
12. 两部门三角色演示数据；
13. 验收清单；
14. 已知问题与技术债务清单；
15. Pull Request 测试结果、迁移说明、接口变化、联调依赖和回滚方式。

不得用以下内容冒充交付：

- 伪代码；
- 固定返回值；
- 空壳接口；
- 永远返回允许的权限函数；
- 只隐藏前端按钮而不校验后端；
- Mock Token 作为正式认证；
- 只在本地存在、未推送到功能分支的代码；
- 压缩包或私下复制目录代替 Pull Request。

## 十一、你现在应按顺序完成的事项

1. 检查 `origin`、当前分支和工作区状态；
2. 从最新 `develop` 创建 `feature/m4-*` 分支；
3. 阅读成员1公共契约和现有身份、权限、日志、迁移基础；
4. 列出已存在能力、缺失能力、公共改动和硬阻塞；
5. 与成员1冻结认证、错误码、审计和 Outbox；
6. 与成员5、6冻结 AccessContext、权限元数据和检索过滤；
7. 建立两部门三角色测试矩阵；
8. 按 M1 至 M4 分阶段开发、测试、提交和创建 Pull Request；
9. 在集成环境完成登录、授权、上传、检索、问答和引用的纵向越权测试；
10. 完成交付文档和验收清单后再标记成员4任务完成。


---

# 企业级混合检索 RAG 项目成员4 Cursor 完整构建提示词

## 角色与总目标

你是本项目的成员4开发执行者，负责“企业级混合检索 RAG 知识问答平台”的身份认证、用户组织、RBAC、数据范围、基础 ABAC、临时授权、PermissionService、权限缓存、权限变更事件、登录审计和安全审计模块。

请基于当前已经克隆的团队共享 Gitea 仓库，完整实现成员4职责范围内的可运行后端模块。交付必须可执行、可落地、可测试、可联调、可通过 Pull Request 合并，不得只生成示例、伪代码、空壳接口、固定返回值或永远返回允许的临时权限逻辑。

所有代码注释、接口说明、README、测试说明、交付报告和页面相关文案使用简体中文。禁止使用 Emoji。

## 一、权威依据与优先级

执行时按以下优先级处理冲突：

1. 当前仓库中的 `AGENTS.md`、项目规则和组长已经冻结的公共契约；
2. `docs/api-contracts.md`、`docs/data-model.md`、`docs/error-codes.md`、`docs/event-contracts.md` 及公共状态枚举；
3. 《成员4_身份权限与组织后端任务书.md》与《Gitea七人协作提交与项目合并指南.md》；
4. 当前提示词。

不得私自改变已冻结的公共字段、状态、错误码、事件信封、数据库公共实体、响应格式、认证中间件接口或迁移规范。

发现契约缺失或冲突时：

1. 先检查仓库中是否存在更新版本；
2. 采用向后兼容方式完成不受影响的内部实现；
3. 将仍无法解决的问题集中写入一份“成员4契约与业务参数阻塞清单”；
4. 不要反复逐个询问，不要因局部阻塞停止整个模块；
5. 不得凭空编造企业角色、部门、密级、真实用户或生产安全参数。

## 二、工作方式

1. 先审计当前仓库，再开始编码，不要假设项目为空；
2. 最大程度自主推进，不频繁询问；
3. 仅在以下硬阻塞情况下停止对应部分：
   - `origin` 与指定共享仓库不一致；
   - 远程 `develop` 不存在且无法确定团队集成分支；
   - 公共认证、错误码或数据库契约互相矛盾且无法兼容；
   - 公共 User、KnowledgeBase、Document 等实体缺失且其归属无法确定；
   - 密级、ABAC 属性或 Token 策略尚未确认，且仓库不存在默认配置；
   - 必要数据库或公共基础设施完全不可用，且无法使用契约一致的测试替身继续完成非生产逻辑。
4. 硬阻塞只阻止受影响部分，其余模型、服务、测试、文档、接口适配和迁移继续完成；
5. 不修改其他成员主责模块的核心业务逻辑；
6. 不删除、弱化、跳过或篡改现有测试；
7. 不为通过测试写固定结果或默认放行；
8. 不提交真实 `.env`、密码、密钥、Token、数据库数据、上传文件、Volume、缓存或构建产物；
9. 不执行危险 Git 命令；
10. 每完成一个小阶段，立即运行对应测试并修复问题；
11. 对公共目录的最小必要修改，必须在交付报告和 Pull Request 中标记成员1评审。

## 三、Git 与仓库保护

团队共享仓库预期地址：

```text
http://192.168.51.25:3010/admin/origin.git
```

开始时依次执行并记录结果：

```bash
git status --short
git branch --show-current
git remote -v
git fetch origin
```

必须遵守：

- 仓库已经克隆，禁止执行 `git init`；
- 使用当前已有的 `origin`；
- 禁止自行添加、删除或修改 `origin`；
- 每位成员使用自己的 Gitea 账号和凭据；
- 禁止直接向 `main` 或 `develop` 推送；
- 禁止 `git push --force`、`git push -f`、`git reset --hard`、`git clean -fd`；
- 功能分支统一使用 `feature/m4-` 前缀；
- Pull Request 的目标分支统一为 `develop`；
- 公共后端、共享认证依赖、根目录依赖和迁移公共规则邀请成员1评审；
- 权限元数据与文档权限邀请成员5；
- 检索过滤、引用校验与缓存隔离邀请成员6；
- 标准问答权限、安全事件和评估权限邀请成员7；
- 用户端认证接口邀请成员2；
- 管理后台用户、角色、权限和审计接口邀请成员3。

如果当前工作区存在用户未提交修改，不覆盖、不丢弃、不重置。先识别修改范围，只在不破坏现有工作的前提下继续。

从最新 `develop` 创建分支：

```bash
git fetch origin
git switch develop
git pull --ff-only origin develop
git switch -c feature/m4-auth-organization
```

如果本地尚无 `develop`，但远程存在：

```bash
git fetch origin
git switch -c develop --track origin/develop
git switch -c feature/m4-auth-organization
```

## 四、仓库审计

先检查并形成简洁审计记录：

```text
AGENTS.md 或项目规则文件
README.md
docs/api-contracts.md
docs/data-model.md
docs/error-codes.md
docs/event-contracts.md
公共状态枚举
pyproject.toml、requirements 或其他依赖文件
FastAPI 应用工厂和路由注册方式
SQLAlchemy Base、Session 和 Alembic 当前 head
公共 User、Tenant、KnowledgeBase、Document 等实体
认证依赖、中间件和安全配置
Redis、Celery、Outbox 和缓存封装
结构化日志、request_id、trace_id 和异常处理
统一审计调用接口
Prometheus 指标命名规则
Docker Compose、健康检查和测试命令
现有身份、权限、组织或审计代码
```

审计输出至少列出：

- 已存在且应复用的能力；
- 成员4范围内缺失的能力；
- 需要成员1评审的公共变更；
- 需要成员2、3、5、6、7签收的契约；
- 待团队确认的业务参数；
- 硬阻塞和可继续推进的部分。

不得重复创建第二套数据库、配置、异常处理、日志、Token 中间件、审计表或公共响应封装。

## 五、代码范围

优先只修改以下成员4主责目录：

```text
backend/app/identity/
backend/app/permissions/
backend/app/audit/
backend/app/models/identity/
backend/app/schemas/identity/
backend/tests/identity/
backend/tests/permissions/
```

成员4拥有以下业务模型和实现：

```text
Tenant
User
Department
UserGroup
Role
Permission
UserRole
RolePermission
UserDepartment
UserGroupMember
DataScope
KnowledgeBasePermission
DocumentPermission
TemporaryGrant
LoginLog
AuditLog
SecurityEvent
```

如果仓库中已有同名或等价实体，复用并扩展，不得创建重复模型或重复表。

如果确需修改公共路由注册、认证依赖、公共模型、API 文档、依赖文件或 Alembic 公共目录：

1. 只做最小必要改动；
2. 不升级无关依赖；
3. 不覆盖其他成员迁移；
4. 记录兼容性和回滚方式；
5. 在 Pull Request 中明确邀请成员1评审。

## 六、禁止擅自确定的内容

以下内容优先读取仓库配置或团队决策，不得自行编造：

- 实际企业名称、人员和组织结构；
- 文档密级名称、数量和等级顺序；
- 一期正式角色和完整权限编码清单；
- 登录失败阈值和限制时长；
- Access Token 与 Refresh Token 生命周期；
- Refresh Token 轮换或失效的具体生产策略；
- 临时授权审批流程；
- 权限变化对正在进行请求的处理策略；
- 生产管理员账号和密码；
- 生产域名、证书、密钥和审计保留周期。

如果仓库未冻结这些值：

- 将可配置项通过项目现有配置框架暴露；
- 在 `.env.example` 或配置文档中只写变量名和安全说明，不写真实值；
- 使用明确标记的虚构演示数据完成测试；
- 在交付报告中列出待团队确认项；
- 不把开发默认值冒充最终生产决策。

## 七、数据模型与迁移

### 7.1 通用要求

所有权限实体遵循仓库既有规范，并至少考虑：

```text
tenant_id
status
effective_time
expiration_time
created_by
updated_by
created_at
updated_at
必要的软删除字段
```

要求：

- 一期为单企业部署，但核心表保留 `tenant_id`；
- 使用 SQLAlchemy 2.x 现有声明方式；
- 外键、唯一约束、索引和引用删除规则明确；
- 时间统一使用公共时区和格式规范；
- 密码和 Token 不明文存储；
- 迁移必须支持 `upgrade` 和 `downgrade`；
- 不修改已有迁移历史；
- 迁移前后运行数据库测试。

### 7.2 关系规则

至少满足：

- 用户可以属于一个主部门；
- 用户可以属于多个用户组；
- 一个用户可以拥有多个角色；
- 组织关系和功能角色分离；
- 角色删除前检查用户角色和角色权限引用；
- Chunk 权限不得高于文档；
- 标准问答权限不得高于其全部来源文档。

## 八、身份认证工作包

完整实现：

```text
用户名和密码登录
密码安全哈希
Access Token
Refresh Token
Refresh Token 轮换或失效机制
退出登录
当前用户
用户禁用
密码重置
登录失败次数限制
登录日志
Token 过期和撤销
未来 SSO 扩展接口
```

必须遵守：

- 密码不得明文保存；
- Token 不写普通日志；
- 禁用用户后现有会话失效；
- Refresh Token 可撤销；
- 登录失败达到配置阈值后限制尝试；
- 审计登录 IP、用户代理和结果；
- 未认证访问返回统一认证错误；
- 认证成功后通过公共依赖注入当前用户；
- 不在成员4模块内重新建立与成员1冲突的第二套认证中间件。

至少实现接口：

```text
POST /api/v1/auth/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
GET  /api/v1/auth/me
```

密码重置、禁用和用户管理接口按仓库已冻结的公共 API 规则实现。

## 九、组织模型工作包

完整实现：

```text
用户 CRUD
部门 CRUD
部门层级
用户组 CRUD
用户加入或移出主部门
用户加入或移出用户组
用户状态管理
用户基础属性
批量导入预留接口
```

组织接口至少覆盖：

```text
/api/v1/users
/api/v1/departments
/api/v1/user-groups
```

要求：

- 支持分页、搜索和筛选；
- 写操作执行功能权限校验；
- 并发修改有冲突处理；
- 禁用用户前后记录审计；
- 删除或停用组织实体时检查引用和影响范围；
- 批量导入一期只保留契约时，不得用固定成功结果冒充实现。

## 十、RBAC 工作包

完整实现：

```text
角色
权限
用户角色
角色权限
菜单权限
API 权限
操作权限
```

权限编码必须稳定且可读。任务书示例包括：

```text
user.read
user.manage
role.manage
knowledge_base.read
knowledge_base.manage
document.upload
document.publish
qa.review
evaluation.run
audit.read
system.configure
```

示例只作为命名风格参考。正式编码清单优先使用仓库已经冻结的权限编码；缺失时只建立任务书所需的最小测试权限集并标记待成员1、3签收，不得擅自扩展企业业务权限。

强制规则：

- 后端使用权限编码执行真实校验；
- 前端菜单由后端权限结果生成；
- 菜单隐藏不能替代 API 鉴权；
- 角色删除前检查引用；
- 权限和角色变化记录审计；
- 系统管理员不默认具备全部业务文档阅读权；
- `can_execute_action()` 是功能权限统一入口。

## 十一、数据范围、ABAC 与临时授权

至少支持：

```text
tenant
部门
用户组
知识库
项目
地区
文档密级
生效时间
失效时间
显式拒绝
临时授权
```

权限计算强制遵守：

1. 默认拒绝；
2. 显式拒绝优先；
3. 功能权限与数据权限分别计算；
4. 多角色允许范围合并；
5. 临时授权只在有效期内生效；
6. 文档密级不得高于用户可访问等级；
7. Chunk 权限不得高于文档；
8. 标准问答权限不得高于全部来源文档；
9. 引用打开时重新校验；
10. 权限变化后使相关缓存失效。

不得将“系统管理员”简单实现为跳过所有数据权限。

## 十二、AccessContext

统一输出至少兼容：

```json
{
  "tenant_id": "",
  "user_id": "",
  "role_ids": [],
  "department_ids": [],
  "group_ids": [],
  "knowledge_base_ids": [],
  "project_ids": [],
  "regions": [],
  "max_confidentiality_level": 0,
  "deny_document_ids": [],
  "temporary_grants": [],
  "scope_hash": ""
}
```

实现要求：

- 使用项目既有 Pydantic 和序列化规范；
- 每次请求计算或取得不可变快照；
- 同一请求内不得一半使用旧权限、一半使用新权限；
- `scope_hash` 由规范化后的权限范围摘要生成；
- `scope_hash` 不包含可逆敏感数据；
- 相同有效权限范围生成稳定摘要；
- 权限变化后旧摘要对应缓存必须失效；
- 当前用户接口可以返回必要权限摘要，但不得泄露内部敏感规则或完整权限表结构。

## 十三、PermissionService

全项目只能存在一套权限规则来源，至少实现：

```text
get_access_context()
get_user_access_scope()
build_retrieval_filters()
can_execute_action()
can_access_knowledge_base()
can_access_document()
can_access_chunk()
can_access_standard_qa()
can_open_citation()
invalidate_user_permission_cache()
```

### 13.1 服务设计要求

- 明确输入、输出和异常；
- 支持依赖注入；
- 只通过仓库规定的数据访问层读取权限数据；
- 避免路由层复制权限计算；
- 每个方法具备单元测试；
- 记录 `request_id`、`trace_id`、用户、动作、资源、结果和拒绝原因；
- 日志不得泄露 Token、密码或敏感正文；
- 权限拒绝使用统一错误码；
- 对重复查询使用 Redis 权限缓存，但必须按 tenant 和用户隔离；
- 缓存值使用 AccessContext 或权限摘要，不缓存明文密码和 Token。

### 13.2 检索过滤

`build_retrieval_filters()` 必须输出成员6可消费的统一语义权限结构，再由适配器转换为：

```text
OpenSearch Filter DSL
PostgreSQL/pgvector WHERE 条件
```

强制要求：

- 两种检索表达相同权限语义；
- 权限过滤发生在正文读取和候选召回之前；
- 过滤至少覆盖 tenant、知识库、部门或用户组范围、项目、地区、密级、有效期、显式拒绝和临时授权；
- 成员6不得重新实现复杂权限规则；
- 如果公共过滤 Schema 尚未冻结，只能实现与 AccessContext 一一对应的最小内部模型，并生成契约草案供成员1、5、6签收，不得私自把草案当成已冻结公共规范。

## 十四、权限快照、缓存和 Outbox 事件

权限变化时必须在同一业务事务中完成：

1. 更新 PostgreSQL 业务状态；
2. 写入 Outbox 事件；
3. 提交成功后失效用户权限缓存；
4. 通知成员6失效查询缓存和答案缓存；
5. 记录 AuditLog；
6. 必要时记录 SecurityEvent；
7. 按团队既定策略处理正在进行的请求。

至少发布：

```text
permission.user.changed
permission.role.changed
permission.knowledge_base.changed
permission.document.changed
temporary_grant.expired
```

要求：

- 事件信封复用成员1规范；
- 生产者事务一致；
- 消费和失效操作幂等；
- 临时授权过期可被可靠发现并发布事件；
- 事件载荷不包含密码、Token 或不必要的敏感个人信息；
- 失败可重试并有日志和指标。

## 十五、审计与安全事件

成员4拥有：

```text
AuditLog
SecurityEvent
LoginLog
权限审计查询接口
登录审计查询接口
越权事件结构和存储
```

成员5、6、7发生业务动作时调用统一审计接口，不得各自创建重复审计表。

审计至少能够记录和筛选：

```text
用户
租户
动作
资源类型
资源标识
结果
拒绝原因
事件类型
IP
用户代理
request_id
trace_id
发生时间
```

具体字段以成员1已冻结规范为准。审计日志需要防止普通业务用户修改或删除，敏感字段按公共规范脱敏。

至少为以下行为产生日志或安全事件：

- 登录成功与失败；
- Token 刷新、撤销和退出；
- 用户禁用和密码重置；
- 角色和权限变化；
- 数据范围变化；
- 临时授权创建、变更和过期；
- 无权限访问；
- 构造 ID 猜测；
- 引用二次校验失败；
- 越权召回、引用、回答或缓存泄露信号。

## 十六、主要 API

认证：

```text
POST /api/v1/auth/login
POST /api/v1/auth/refresh
POST /api/v1/auth/logout
GET  /api/v1/auth/me
```

组织：

```text
/api/v1/users
/api/v1/departments
/api/v1/user-groups
```

权限：

```text
/api/v1/roles
/api/v1/permissions
/api/v1/data-scopes
/api/v1/temporary-grants
/api/v1/access-context
/api/v1/permission-audits
```

所有列表接口遵循公共分页格式，并支持任务书要求的搜索和筛选。所有写操作执行操作权限校验、事务、冲突处理和审计。

不要自行发明与 `docs/api-contracts.md` 冲突的路由、响应包装、状态码或错误码。

## 十七、跨模块接口与联调

### 17.1 向成员2提供

```text
登录
刷新 Token
退出
当前用户
角色和权限摘要
可访问知识库摘要
临时授权及到期时间
账号禁用、Token 过期和权限不足错误
```

认证失效后，接口必须支持成员2统一跳转登录和清理前端缓存。

### 17.2 向成员3提供

```text
用户和组织 CRUD
角色和权限配置
用户角色和用户组分配
功能权限和数据权限
临时授权
最终权限摘要
当前管理员菜单和操作权限
登录、权限和安全审计查询
```

不同后台角色必须看到正确菜单和按钮，但后端仍执行真实校验。

### 17.3 向成员5提供

```text
知识库默认权限
文档权限绑定
Chunk 权限继承规则
文档访问校验
索引权限元数据语义
文档权限变化事件
```

不得直接修改成员5的文档解析、切分和索引核心逻辑。

### 17.4 向成员6提供

```text
AccessContext
build_retrieval_filters()
can_access_chunk()
can_access_standard_qa()
can_open_citation()
scope_hash
权限变更和临时授权过期事件
```

联调必须证明：

- 权限过滤在 Keyword 和 Vector 检索之前生效；
- 显式拒绝文档不进入候选；
- 引用返回前再次校验；
- 权限变化后查询和答案缓存失效；
- 不同 `scope_hash` 不能共享敏感答案缓存。

### 17.5 向成员7提供

```text
标准问答权限校验
允许和禁止角色校验
部门和适用范围校验
评估权限上下文
越权安全事件数据
权限相关指标
```

评估必须通过正式权限链路，不允许成员7直接绕过 PermissionService 读取底层索引。

## 十八、测试数据

建立契约一致的虚构测试数据，至少覆盖：

```text
两个部门
三种角色
公共知识库
部门私有知识库
显式拒绝文档
有效临时授权
过期临时授权
不同密级
禁用用户
多角色用户
```

不得使用真实企业账号、真实密码、真实文档或生产数据。种子数据的密码处理遵循项目安全规范，并在 README 中明确只用于开发和测试。

## 十九、测试要求

### 19.1 单元测试

至少覆盖：

- 密码哈希与校验；
- Token 签发、过期、撤销和刷新；
- 登录失败计数和限制；
- 用户禁用；
- 组织关系；
- 角色和权限合并；
- 默认拒绝；
- 显式拒绝优先；
- 临时授权有效和过期；
- 密级判断；
- AccessContext 生成；
- `scope_hash` 稳定性和隔离性；
- PermissionService 全部方法；
- 检索过滤语义；
- 权限缓存和失效；
- Outbox 事件生成；
- 审计与 SecurityEvent。

### 19.2 API 集成测试

至少覆盖：

- 登录、刷新、退出和当前用户；
- 无 Token 和过期 Token；
- 被撤销 Refresh Token；
- 用户、部门、用户组 CRUD；
- 角色、权限和分配；
- 数据范围和临时授权；
- AccessContext；
- 权限审计查询；
- 分页、筛选和冲突；
- 普通用户访问后台 API；
- 构造 ID 猜测。

### 19.3 权限矩阵和安全测试

必须通过：

```text
A 部门用户访问 B 部门文档被拒绝
公共知识库按规则允许
部门私有知识库隔离
显式拒绝优先于允许
临时授权在有效期内生效
临时授权过期后立即失效
超过密级上限被拒绝
禁用用户现有会话失效
多角色允许范围正确合并
系统管理员无业务阅读权时不能读取文档
Chunk 不得高于文档权限
标准问答不得高于全部来源文档权限
无权引用不能打开且不泄露真实地址
权限变化后权限缓存失效
权限变化后成员6查询和答案缓存失效
不同 scope_hash 不发生缓存串用
越权召回、引用和回答为零
```

### 19.4 质量门禁

使用仓库现有命令。至少运行适用的：

```text
Ruff
mypy 约定范围
pytest 定向测试
pytest 全量后端测试
Alembic upgrade
Alembic downgrade
Docker Compose 配置校验
关键接口冒烟测试
```

不得通过删除测试、弱化断言、标记跳过或固定返回结果掩盖缺陷。

## 二十、日志、指标与安全

所有关键请求使用统一 `request_id` 和 `trace_id`。

结构化日志至少覆盖：

```text
认证阶段
权限计算阶段
缓存命中或失效
动作权限判断
资源权限判断
检索过滤生成
引用校验
Outbox 写入和处理
审计写入
异常和拒绝原因
阶段耗时
```

不得记录：

```text
明文密码
完整 Access Token
完整 Refresh Token
真实密钥
不必要的敏感个人信息
未经授权的文档正文
```

按成员1指标命名规范产生本模块 Prometheus 指标，并供成员7聚合。至少关注登录成功与失败、认证拒绝、权限拒绝、权限计算耗时、缓存命中、缓存失效、临时授权过期、Outbox 失败和安全事件数量。

## 二十一、实现里程碑

### M1：认证与组织

完成：

```text
仓库审计
模型与迁移
登录和 Token
用户、部门、用户组
登录限制和日志
成员2认证接口联调
单元与 API 测试
```

建议分支：

```text
feature/m4-auth-organization
```

### M2：RBAC

完成：

```text
角色和权限
用户角色和角色权限
菜单、API 和操作权限
can_execute_action()
成员3后台联调
审计和测试
```

建议分支：

```text
feature/m4-rbac
```

### M3：数据范围和 PermissionService

完成：

```text
DataScope
KnowledgeBasePermission
DocumentPermission
显式拒绝
TemporaryGrant
AccessContext
scope_hash
PermissionService
权限缓存
检索过滤契约
```

建议分支：

```text
feature/m4-data-scope-permission-service
```

### M4：权限事件、审计与纵向联调

完成：

```text
AuditLog
SecurityEvent
Outbox 权限事件
成员5文档权限联调
成员6检索和引用联调
成员7标准问答和评估联调
越权与缓存泄露测试
交付文档和验收
```

建议分支：

```text
feature/m4-permission-integration-audit
```

每个里程碑完成后：

1. 运行定向测试；
2. 修复失败；
3. 更新接口文档；
4. 提交小粒度 Commit；
5. 推送个人功能分支；
6. 创建以 `develop` 为目标的 Pull Request；
7. 邀请相关成员评审。

## 二十二、文档交付

至少新增或更新：

```text
成员4模块 README
认证与 Token 说明
组织模型说明
RBAC 权限编码说明
数据范围和 ABAC 说明
AccessContext 契约
PermissionService 接口说明
检索过滤契约
缓存与失效说明
Outbox 事件说明
审计与 SecurityEvent 说明
环境变量和配置说明
迁移与回滚说明
测试说明
联调说明
验收清单
已知问题与技术债务
```

公共文档仅做最小必要更新，并遵守成员1评审要求。

## 二十三、Pull Request 要求

每个 Pull Request 必须填写：

```markdown
## 任务来源
成员编号：成员4
任务书章节：
关联 Issue：

## 完成内容
-

## 修改范围
-

## 接口或数据模型变化
-

## 数据库迁移
无 / 有，说明迁移文件、upgrade、downgrade 和影响

## 环境变量变化
无 / 有，只填写变量名，不填写密钥

## 测试
执行命令：
测试结果：

## 联调要求
依赖模块：
需要谁参与：

## 风险与回滚
风险：
回滚方式：

## 检查
- [ ] 未提交真实 .env、密码、密钥、Token、数据和构建产物
- [ ] 未直接修改其他成员主责模块
- [ ] 接口文档已同步
- [ ] 数据库迁移可升级和回退
- [ ] 定向测试通过
- [ ] 已同步最新 develop
- [ ] 权限默认拒绝和显式拒绝优先已验证
```

提交信息使用：

```text
feat(m4): <简明说明>
fix(m4): <简明说明>
refactor(m4): <简明说明>
test(m4): <简明说明>
docs(m4): <简明说明>
chore(m4): <简明说明>
```

## 二十四、完成定义

只有同时满足以下条件，成员4任务才可标记完成：

- 认证、组织、RBAC、数据范围、临时授权和审计功能全部实现；
- 全项目只有一套 PermissionService 权限规则来源；
- AccessContext 和 `scope_hash` 可真实计算；
- 权限过滤可被 OpenSearch 与 pgvector 使用；
- 文档、Chunk、标准问答、引用和缓存权限一致；
- 无权限默认拒绝，显式拒绝优先；
- 系统管理员不被错误赋予全部业务阅读权；
- 权限变化通过 Outbox 和缓存失效及时生效；
- 关键失败路径得到处理；
- 越权召回、引用、回答和跨权限缓存泄露目标为零；
- 代码通过格式化、静态检查和测试；
- 数据库迁移可升级和回退；
- 与成员2、3、5、6、7完成真实或契约一致的联调；
- 集成环境冒烟测试通过；
- 接口文档与实际实现一致；
- 验收清单、已知问题和技术债务完整；
- 代码已经推送到 `feature/m4-*` 分支并创建以 `develop` 为目标的 Gitea Pull Request。

## 二十五、最终输出格式

完成执行后，输出一份“成员4交付报告”，按以下结构汇总：

```text
1. 仓库与分支状态
2. 已完成能力
3. 未完成能力及真实原因
4. 新增和修改文件
5. 数据库迁移
6. API 与契约
7. 权限计算和安全规则
8. 测试命令与结果
9. 跨成员联调结果
10. 配置与环境变量
11. 指标、日志和审计
12. Pull Request 信息
13. 待团队确认参数
14. 已知问题和技术债务
15. 最终验收结论
```

不要只报告“已完成”。必须给出可核验的文件、命令、测试结果和未完成项。不得声称未执行的测试已经通过，不得把契约 Mock 描述为正式生产实现。
