# Grafana 配置
apiVersion: 1

providers:
  - name: 'RAG Knowledge'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
