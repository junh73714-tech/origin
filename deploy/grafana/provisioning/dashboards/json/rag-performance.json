{
  "dashboard": {
    "title": "RAG 链路性能",
    "uid": "m7-rag-performance",
    "tags": ["m7", "rag", "performance"],
    "panels": [
      {
        "title": "关键词检索耗时",
        "type": "graph",
        "gridPos": {"x": 0, "y": 0, "w": 8, "h": 8},
        "targets": [{"expr": "histogram_quantile(0.95, sum(rate(rag_keyword_retrieval_duration_seconds_bucket[5m])) by (le))", "legendFormat": "P95"}]
      },
      {
        "title": "向量检索耗时",
        "type": "graph",
        "gridPos": {"x": 8, "y": 0, "w": 8, "h": 8},
        "targets": [{"expr": "histogram_quantile(0.95, sum(rate(rag_vector_retrieval_duration_seconds_bucket[5m])) by (le))", "legendFormat": "P95"}]
      },
      {
        "title": "Reranker 耗时",
        "type": "graph",
        "gridPos": {"x": 16, "y": 0, "w": 8, "h": 8},
        "targets": [{"expr": "histogram_quantile(0.95, sum(rate(rag_reranker_request_duration_seconds_bucket[5m])) by (le, provider))", "legendFormat": "P95 - {{provider}}"}]
      },
      {
        "title": "LLM 调用耗时",
        "type": "graph",
        "gridPos": {"x": 0, "y": 8, "w": 12, "h": 8},
        "targets": [{"expr": "histogram_quantile(0.95, sum(rate(rag_llm_request_duration_seconds_bucket[5m])) by (le, operation))", "legendFormat": "P95 - {{operation}}"}]
      },
      {
        "title": "Embedding 调用耗时",
        "type": "graph",
        "gridPos": {"x": 12, "y": 8, "w": 12, "h": 8},
        "targets": [{"expr": "histogram_quantile(0.95, sum(rate(rag_embedding_request_duration_seconds_bucket[5m])) by (le, provider))", "legendFormat": "P95 - {{provider}}"}]
      }
    ]
  }
}