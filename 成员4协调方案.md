# 📊 成员4（身份认证与权限系统）协调方案

> 版本：v1.0  
> 日期：2026-07-16  
> 作者：成员4  
> PR链接：http://192.168.51.25:3010/admin/origin/pulls/new?head=feature/member4-identity-auth&base=main

---

## 一、成员职责概览

| 成员 | 职责领域 | 关键交付物 |
|------|----------|------------|
| 成员1 | 基础架构 | 数据库配置、核心模块、依赖注入 |
| 成员2 | API网关 | 路由代理、负载均衡、请求转发 |
| 成员3 | 前端页面 | 登录页、权限控制、界面展示 |
| 成员4 | 身份认证 | JWT、RBAC、数据权限、审计日志 |
| 成员5 | 搜索服务 | OpenSearch索引、搜索接口 |
| 成员6 | 混合检索 | RAG检索流程、向量+关键词 |
| 成员7 | 其他模块 | 文档处理、反馈系统等 |

---

## 二、各成员具体协调方案

### 🎯 成员1（基础架构）协调

**集成点**：数据库迁移、配置管理、核心依赖

**操作步骤**：

1. **执行数据库迁移**
   ```bash
   cd backend
   alembic upgrade head
   ```

2. **验证新增表结构**
   ```sql
   -- 检查新增的身份认证相关表
   \dt departments
   \dt user_groups
   \dt temporary_grants
   \dt login_logs
   \dt audit_logs
   ```

3. **配置Redis连接**（用于权限缓存）
   - 修改 `.env` 文件：
   ```
   REDIS_HOST=localhost
   REDIS_PORT=6379
   REDIS_PASSWORD=
   ```

4. **确认数据库连接配置**
   - 检查 `backend/app/core/config.py` 中的数据库URL生成逻辑
   - 确保 `tenant_id` 字段在所有模型中正确使用

**依赖确认**：
- ✅ SQLAlchemy 2.x 异步支持
- ✅ PostgreSQL JSONB 支持
- ✅ Redis 连接池配置

**涉及文件**：
- `backend/alembic/versions/002_identity_permission_tables.py` - 数据库迁移脚本
- `backend/app/core/config.py` - 配置管理
- `backend/app/core/database.py` - 数据库连接

---

### 🎯 成员2（API网关）协调

**集成点**：认证路由、JWT验证、权限中间件

**操作步骤**：

1. **代理认证相关路由**
   ```
   /api/auth/*        → 成员4认证服务
   /api/rbac/*        → 成员4RBAC服务
   /api/organization/* → 成员4组织管理
   /api/data-permission/* → 成员4数据权限
   /api/audit/*       → 成员4审计服务
   ```

2. **配置JWT验证中间件**
   - 从请求头提取 `Authorization: Bearer <token>`
   - 调用 `backend/app/core/security.py` 中的 `verify_token()` 验证
   - 将解析后的 `user_id`、`tenant_id` 注入请求上下文

3. **权限检查集成**
   - 在网关层实现 `require_permission()` 装饰器
   - 参考 `backend/app/core/dependencies.py` 中的权限依赖

4. **配置CORS允许凭证**
   - 确保前端域名在 `cors_origins` 配置中

**关键接口**：
| 接口 | 用途 |
|------|------|
| `POST /api/auth/login` | 用户登录 |
| `POST /api/auth/refresh` | 刷新令牌 |
| `POST /api/auth/logout` | 退出登录 |
| `GET /api/auth/me` | 当前用户 |

**涉及文件**：
- `backend/app/core/security.py` - JWT验证
- `backend/app/core/dependencies.py` - 依赖注入
- `backend/app/api/auth.py` - 认证路由

---

### 🎯 成员3（前端）协调

**集成点**：登录流程、权限控制、界面展示

**操作步骤**：

1. **实现登录页面**
   - 调用 `POST /api/auth/login` 获取token
   - 将token存储到localStorage/sessionStorage
   - 格式：`access_token`、`refresh_token`、`expires_at`

2. **实现认证状态管理**
   - 创建AuthStore管理登录状态
   - 实现自动刷新token逻辑
   - 401时自动跳转登录页

3. **实现权限控制**
   - 调用 `GET /api/auth/me` 获取用户信息和权限
   - 根据权限控制菜单显示/隐藏
   - 使用 `POST /api/rbac/permission-check` 检查操作权限

4. **组织管理页面**
   - 部门树形结构展示（`GET /api/organization/departments/tree`）
   - 用户组管理（`GET/POST /api/organization/user-groups`）
   - 用户-部门/组关系管理

5. **RBAC管理页面**
   - 角色列表与编辑
   - 权限列表与分配
   - 用户角色分配

6. **审计日志页面**
   - 登录日志查询
   - 操作审计记录
   - 安全事件管理

**数据结构参考**：
```typescript
// 用户信息
interface UserInfo {
  id: string;
  username: string;
  email: string;
  roles: string[];
  permissions: string[];
  tenant_id: string;
}

// 权限检查
interface PermissionCheck {
  action: string;
  resource_type: string;
  resource_id?: string;
}
```

**涉及文件**：
- `backend/app/api/auth.py` - 认证路由
- `backend/app/api/rbac.py` - RBAC路由
- `backend/app/api/organization.py` - 组织管理路由
- `backend/app/api/audit.py` - 审计路由

---

### 🎯 成员5（搜索服务）协调

**集成点**：权限过滤、文档访问控制

**操作步骤**：

1. **集成检索过滤条件**
   - 在搜索请求中接收 `scope_hash` 参数
   - 调用 `backend/app/services/permission_service.py` 的 `build_opensearch_filter()` 生成过滤条件

2. **文档权限过滤**
   - 在搜索结果中过滤掉用户无权访问的文档
   - 使用 `deny_document_ids` 排除显式拒绝的文档
   - 使用 `knowledge_base_ids` 限制知识库范围

3. **索引结构改造**
   - 在文档索引中添加 `tenant_id`、`department_id`、`confidentiality_level` 字段
   - 添加 `accessible_user_ids`、`accessible_group_ids` 字段

4. **搜索接口修改**
   - 在搜索请求中包含用户权限上下文
   - 返回结果时验证文档访问权限

**过滤条件示例**：
```json
{
  "bool": {
    "must": [
      { "term": { "tenant_id": "tenant_001" } },
      { "terms": { "knowledge_base_ids": ["kb_001", "kb_002"] } }
    ],
    "must_not": [
      { "terms": { "id": ["doc_blocked_001"] } }
    ]
  }
}
```

**涉及文件**：
- `backend/app/services/permission_service.py` - 权限服务
- `backend/app/schemas/identity.py` - 权限相关Schema

---

### 🎯 成员6（混合检索）协调

**集成点**：检索流程权限校验、上下文过滤

**操作步骤**：

1. **检索前权限检查**
   - 在检索流程开始前调用 `can_execute_action()` 检查权限
   - 获取用户的 `AccessContext`

2. **向量检索过滤**
   - 根据 `knowledge_base_ids` 限制向量查询范围
   - 根据 `max_confidentiality_level` 过滤文档级别

3. **关键词检索过滤**
   - 应用与成员5相同的权限过滤条件
   - 确保混合检索结果符合权限要求

4. **结果合并过滤**
   - 在结果合并阶段再次验证权限
   - 去除不符合权限的文档

5. **检索接口改造**
   - 接收 `user_id` 和 `scope_hash` 参数
   - 调用 `build_retrieval_filters()` 获取过滤条件

**集成流程**：
```
用户请求 → 权限校验 → 向量检索(权限过滤) → 关键词检索(权限过滤) → 结果合并(权限验证) → 返回
```

**涉及文件**：
- `backend/app/services/permission_service.py` - 权限服务
- `backend/app/core/security.py` - AccessContext

---

### 🎯 成员7（其他模块）协调

**集成点**：文档上传权限、反馈权限、知识基地管理

**操作步骤**：

1. **文档上传权限**
   - 上传前检查 `document:create` 权限
   - 设置文档的 `tenant_id`、`department_id`
   - 根据上传者权限设置文档可见范围

2. **反馈系统权限**
   - 检查 `feedback:create`、`feedback:read` 权限
   - 限制用户只能查看自己的反馈或有权限的反馈

3. **知识基地管理**
   - 检查 `knowledge_base:manage` 权限
   - 在知识库创建时关联权限记录

**涉及文件**：
- `backend/app/services/data_permission_service.py` - 数据权限服务
- `backend/app/services/rbac_service.py` - RBAC服务

---

## 三、依赖关系图

```
成员4 (身份认证)
    │
    ├──→ 成员1 (数据库迁移、配置)
    │
    ├──→ 成员2 (API网关、认证中间件)
    │
    ├──→ 成员3 (前端登录、权限UI)
    │
    ├──→ 成员5 (搜索过滤、索引权限)
    │
    ├──→ 成员6 (检索权限校验)
    │
    └──→ 成员7 (文档权限、反馈权限)
```

## 四、协调时间线

| 阶段 | 时间 | 关键任务 |
|------|------|----------|
| **Phase 1** | Day 1 | 成员1完成数据库迁移；成员2配置认证路由 |
| **Phase 2** | Day 2 | 成员3实现登录页面；成员5/6集成权限过滤 |
| **Phase 3** | Day 3 | 联调测试；权限校验全链路测试 |
| **Phase 4** | Day 4 | 修复问题；优化性能；安全审计 |

## 五、联调测试清单

- [ ] JWT令牌颁发与验证
- [ ] 登录/退出流程
- [ ] 角色权限分配
- [ ] 数据权限过滤
- [ ] 搜索结果权限校验
- [ ] 检索流程权限控制
- [ ] 前端权限UI控制
- [ ] 审计日志记录

## 六、核心功能模块清单

| 模块 | 文件 | 功能描述 |
|------|------|----------|
| **身份认证** | `backend/app/services/auth_service.py` | JWT令牌、登录锁定、密码管理 |
| **组织管理** | `backend/app/services/organization_service.py` | 部门树、用户组、成员管理 |
| **RBAC** | `backend/app/services/rbac_service.py` | 角色、权限、用户角色分配 |
| **权限计算** | `backend/app/services/permission_service.py` | AccessContext、检索过滤、scope_hash |
| **数据权限** | `backend/app/services/data_permission_service.py` | 知识库/文档权限、临时授权 |
| **审计日志** | `backend/app/services/audit_service.py` | 登录日志、安全事件、审计记录 |
| **缓存服务** | `backend/app/services/cache_service.py` | Redis权限缓存、Pub/Sub失效 |
| **事件服务** | `backend/app/services/permission_event_service.py` | Outbox事件驱动 |

## 七、API接口清单

### 认证接口
| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/auth/login` | 用户登录 |
| POST | `/api/auth/refresh` | 刷新令牌 |
| POST | `/api/auth/logout` | 退出登录 |
| GET | `/api/auth/me` | 当前用户信息 |
| POST | `/api/auth/reset-password` | 重置密码 |

### RBAC接口
| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/rbac/permissions` | 创建权限 |
| GET | `/api/rbac/permissions` | 获取权限列表 |
| GET | `/api/rbac/permissions/{id}` | 获取权限详情 |
| PUT | `/api/rbac/permissions/{id}` | 更新权限 |
| DELETE | `/api/rbac/permissions/{id}` | 删除权限 |
| POST | `/api/rbac/roles` | 创建角色 |
| GET | `/api/rbac/roles` | 获取角色列表 |
| GET | `/api/rbac/roles/{id}` | 获取角色详情 |
| PUT | `/api/rbac/roles/{id}` | 更新角色 |
| DELETE | `/api/rbac/roles/{id}` | 删除角色 |
| POST | `/api/rbac/users/{user_id}/roles` | 分配角色给用户 |
| DELETE | `/api/rbac/users/{user_id}/roles/{role_id}` | 移除用户角色 |
| POST | `/api/rbac/roles/{role_id}/permissions` | 分配权限给角色 |
| DELETE | `/api/rbac/roles/{role_id}/permissions/{perm_id}` | 移除角色权限 |
| POST | `/api/rbac/permission-check` | 检查权限 |

### 组织管理接口
| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/organization/departments` | 创建部门 |
| GET | `/api/organization/departments` | 获取部门列表 |
| GET | `/api/organization/departments/tree` | 获取部门树 |
| GET | `/api/organization/departments/{id}` | 获取部门详情 |
| PUT | `/api/organization/departments/{id}` | 更新部门 |
| DELETE | `/api/organization/departments/{id}` | 删除部门 |
| POST | `/api/organization/user-groups` | 创建用户组 |
| GET | `/api/organization/user-groups` | 获取用户组列表 |
| PUT | `/api/organization/user-groups/{id}` | 更新用户组 |
| DELETE | `/api/organization/user-groups/{id}` | 删除用户组 |
| POST | `/api/organization/departments/{id}/users` | 分配用户到部门 |
| DELETE | `/api/organization/departments/{id}/users/{user_id}` | 移除部门用户 |

### 数据权限接口
| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/data-permission/knowledge-base-permissions` | 创建知识库权限 |
| POST | `/api/data-permission/document-permissions` | 创建文档权限 |
| POST | `/api/data-permission/temporary-grants` | 创建临时授权 |
| GET | `/api/data-permission/temporary-grants` | 获取临时授权列表 |
| PUT | `/api/data-permission/temporary-grants/{id}` | 更新临时授权 |
| DELETE | `/api/data-permission/temporary-grants/{id}` | 删除临时授权 |

### 审计接口
| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/audit/audit-logs` | 查询审计日志 |
| GET | `/api/audit/security-events` | 查询安全事件 |
| POST | `/api/audit/security-events/{id}/resolve` | 标记安全事件已解决 |
| GET | `/api/audit/login-logs` | 查询登录日志 |

## 八、测试结果

```
27 passed, 6 warnings
```

| 测试文件 | 测试数 | 状态 |
|----------|--------|------|
| test_auth_service.py | 3 | ✅ |
| test_rbac_service.py | 4 | ✅ |
| test_permission_service.py | 4 | ✅ |
| test_config.py | 4 | ✅ |
| test_security.py | 6 | ✅ |
| test_responses.py | 4 | ✅ |

## 九、紧急联系方式

如遇集成问题，优先联系成员4确认接口定义和权限逻辑。

---

## 附录：权限编码规范

| 资源类型 | 操作 | 权限编码示例 |
|----------|------|--------------|
| document | create/read/update/delete | `document:create` |
| knowledge_base | manage/read/write | `knowledge_base:manage` |
| user | manage/create/update | `user:manage` |
| role | manage/create/update | `role:manage` |
| permission | manage | `permission:manage` |
| feedback | create/read | `feedback:create` |

---

**文档结束**